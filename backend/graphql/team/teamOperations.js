const { MemberModel } = require('../../dynamodb/models/memberModel');

class TeamOperations {
  //////////////////////////////////////////////////////////////////
  /////////////////////////// QUERIES //////////////////////////////
  //////////////////////////////////////////////////////////////////
  async teamGetAll() {
    // Teams exist only as name attributes on members
    const uniqueTeams = new Set();
    const allMembers = await MemberModel.scan().exec();
    allMembers.forEach((member) => uniqueTeams.add(member.team));
    return Array.from(uniqueTeams);
  }

  //////////////////////////////////////////////////////////////////
  /////////////////////////// MUTATIONS ////////////////////////////
  //////////////////////////////////////////////////////////////////
  teamCreate(input) {
    // Create a team in S3

    return null;
  }

  //////////////////////////////////////////////////////////////////
  /////////////////////////// HELPERS //////////////////////////////
  //////////////////////////////////////////////////////////////////
}

module.exports.TeamOperations = TeamOperations;
